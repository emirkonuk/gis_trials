version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: gis_qdrant
    ports:
      - "6333:6333"
    volumes:
      - ../data/qdrant:/qdrant/storage

  retrieval_gpu:
    build:
      context: ..
      dockerfile: docker/retrieval.Dockerfile
    image: gis_retrieval:cu124
    container_name: gis_retrieval_gpu
    privileged: true
    gpus: all
    environment:
      SEARCH_GPU: ${SEARCH_GPU:-1}
      CUDA_VISIBLE_DEVICES: ${SEARCH_GPU:-1}
      NVIDIA_VISIBLE_DEVICES: ${SEARCH_GPU:-1}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-all}
      HF_HOME: /workspace/models
      TRANSFORMERS_CACHE: /workspace/models
      GIS_STACK_ROOT: /workspace
      DATA_ROOT: /workspace/data
    volumes:
      - ../data:/workspace/data
      - ../app/retrieval:/workspace/app/retrieval
      - ../app/models:/workspace/models
    working_dir: /workspace/app/retrieval
    depends_on:
      - qdrant
